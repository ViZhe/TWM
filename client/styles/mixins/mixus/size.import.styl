
/**
 * Size
 *
 * size 10px               => width:10px; height:10px
 * size 10px 15px          => width:10px; height:15px
 *
 */

size(arg...)
    if arg && length(arg) == 1
        width arg[0]
        height arg[0]

    else if length(arg) == 2
        width arg[0]
        height arg[1]
